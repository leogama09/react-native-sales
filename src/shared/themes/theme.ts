import { buttonsTheme } from './buttons';
import { grayTheme, mainTheme, neutralTheme } from './colors';

export const theme = {
  colors: {
    grayTheme,
    neutralTheme,
    mainTheme,
  },
  buttons: {
    buttonsTheme,
  },
};
